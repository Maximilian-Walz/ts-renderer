{
  "name": "ts-engine",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "rm -rf node_modules && rm -rf **/node_modules && rm -rf yarn.lock"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  },
  "packageManager": "yarn@4.9.2"
}
